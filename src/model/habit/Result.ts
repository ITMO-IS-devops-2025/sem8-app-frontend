export class Result {
    id: number
    date: string
    value: string

    constructor(id: number, date: string, value: string) {
        this.id = id
        this.date = date
        this.value = value
    }
}